{
  "name": "hearth-core",
  "version": "0.1.0",
  "description": "可审计、可控、允许沉默的 AI 主动唤醒内核",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test --test-isolation=none",
    "check": "node --check src/server.js && node --check src/core.js"
  },
  "engines": { "node": ">=20" },
  "license": "Apache-2.0"
}
