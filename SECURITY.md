# 安全说明

## 报告漏洞

请使用 GitHub 的私密漏洞报告（Security → Report a vulnerability），不要在公开 Issue 中披露可利用细节、密钥或私人事件内容。

## 部署要求

- 上线前必须更换 `ADMIN_TOKEN`，并使用 HTTPS 反向代理；不要把 3520 端口直接暴露到公网。
- `.env`、`data/` 和日志都可能包含敏感信息，不应提交到 Git。
- 只把可信地址配置为 `WEBHOOK_URL`。Hearth Core 会向该地址发送决策内容。
- 外部适配器应采用最小权限、短期凭据、幂等和可撤销设计。
- 这是实验性软件，不应作为医疗、安全或紧急事件的唯一通知渠道。

## 默认降级原则

模型不可用、输出不可解析、动作未授权、冷却中或达到每日上限时，系统应保持沉默并留下审计事件，而不是猜测执行。
